# 🏗️ Architecture & System Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     SAMP ARCHITECT SYSTEM                       │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                      CLIENT APPLICATIONS                         │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Main App (index.html)          Admin Dashboard (admin.html)    │
│  ├─ User Login/Signup           ├─ Admin Login                  │
│  ├─ Script Configuration        ├─ Purchase Queue               │
│  ├─ Code Generation             ├─ QR Code Generator            │
│  ├─ AI Refinement               ├─ Approve/Reject               │
│  ├─ Chat Support                └─ Rejection Reason             │
│  └─ Premium Code Redemption                                     │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
           │                                    │
           │ HTTP/HTTPS                         │ HTTP/HTTPS
           │                                    │
           ▼                                    ▼
┌──────────────────────────────────────────────────────────────────┐
│                    NETLIFY FUNCTIONS API                         │
├──────────────────────────────────────────────────────────────────┤
│  POST /.netlify/functions/api                                    │
│  ├─ auth (login/signup)                                          │
│  ├─ save_prompt (code history)                                   │
│  ├─ chat_send (chat delivery)                                    │
│  ├─ redeem_premium (code redemption)          [NEW]             │
│  ├─ validate_purchase (approve/reject)        [NEW]             │
│  ├─ get_pending_purchases (admin queue)       [NEW]             │
│  └─ generate_premium_key (manual generation)  [NEW]             │
└──────────────────────────────────────────────────────────────────┘
           │
           │ SQL Queries
           │
           ▼
┌──────────────────────────────────────────────────────────────────┐
│                  NEON PostgreSQL DATABASE                        │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │     users        │  │ prompt_history   │  │ chat_history │  │
│  ├──────────────────┤  ├──────────────────┤  ├──────────────┤  │
│  │ username         │  │ username         │  │ username     │  │
│  │ email            │  │ prompt [code]    │  │ message      │  │
│  │ password         │  │ config (JSON)    │  │ code         │  │
│  │ is_premium [NEW] │  │ timestamp        │  │ timestamp    │  │
│  │ premium_code[NEW]│  │                  │  │ status       │  │
│  └──────────────────┘  └──────────────────┘  └──────────────┘  │
│                                                                  │
│  ┌──────────────────────────┐  ┌─────────────────────────────┐  │
│  │   premium_keys [NEW]     │  │   purchases [NEW]           │  │
│  ├──────────────────────────┤  ├─────────────────────────────┤  │
│  │ key (PRM-XXXXX...)       │  │ purchase_id                 │  │
│  │ status (active/redeemed) │  │ buyer_email                 │  │
│  │ created_at               │  │ amount                      │  │
│  │ redeemed_by              │  │ gcash_ref                   │  │
│  │ redeemed_at              │  │ status (pending/approved)   │  │
│  └──────────────────────────┘  │ key (FK → premium_keys)     │  │
│                                 │ rejection_reason            │  │
│                                 │ created_at / validated_at   │  │
│                                 └─────────────────────────────┘  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### 1. User Premium Code Redemption Flow

```
┌─────────────────────────────────────────────────────────────┐
│ USER: Receive Premium Code from Admin                       │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
              ┌──────────────────────────┐
              │ User opens Chat Support  │
              │ in main app              │
              └──────────────────────────┘
                          │
                          ▼
              ┌──────────────────────────┐
              │ Enter code in            │
              │ "Redeem Premium Code"    │
              │ section                  │
              └──────────────────────────┘
                          │
                          ▼
              ┌──────────────────────────┐
              │ Click "Redeem" button    │
              └──────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ API: POST /.netlify/functions/api                           │
│ {                                                           │
│   "action": "redeem_premium",                               │
│   "username": "user",                                       │
│   "code": "PRM-XXXXX..."                                    │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ DATABASE:                                                   │
│ 1. Check: premium_keys WHERE key=code AND status='active'   │
│ 2. If exists:                                               │
│    UPDATE premium_keys SET status='redeemed',               │
│                            redeemed_by='user'               │
│    UPDATE users SET is_premium=true                         │
│    INSERT INTO premium_keys (auto-generate new key)         │
│ 3. Return success ✓                                         │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
              ┌──────────────────────────┐
              │ Chat shows: "✓ Premium   │
              │ activated!"              │
              └──────────────────────────┘
                          │
                          ▼
              ┌──────────────────────────┐
              │ User account upgraded    │
              │ to premium instantly     │
              └──────────────────────────┘
```

### 2. GCash Purchase Validation Flow

```
┌────────────────────────────────────────────────────────────────┐
│ CUSTOMER: Makes GCash Payment                                  │
└────────────────────────────────────────────────────────────────┘
                             │
                             ▼
                ┌─────────────────────────┐
                │ Payment system creates  │
                │ purchase record         │
                │ (status: pending)       │
                └─────────────────────────┘
                             │
                             ▼
┌────────────────────────────────────────────────────────────────┐
│ DATABASE: Insert into purchases                                │
│ purchase_id: PURCH-001                                         │
│ buyer_email: customer@email.com                                │
│ amount: 500                                                    │
│ gcash_ref: REF123456                                           │
│ status: pending                                                │
└────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌────────────────────────────────────────────────────────────────┐
│ ADMIN: Goes to /admin/ dashboard                               │
│ 1. Logs in with ADMIN_KEY                                      │
│ 2. Sees pending purchases in queue                             │
│ 3. Selects purchase PURCH-001                                  │
│ 4. Reviews details & GCash reference                           │
│ 5. (Optional) Generates & displays payment QR code             │
│ 6. (Optional) Uploads GCash receipt screenshot                 │
└────────────────────────────────────────────────────────────────┘
                             │
           ┌─────────────────┴─────────────────┐
           │                                   │
      APPROVE                             REJECT
           │                                   │
           ▼                                   ▼
     ┌──────────────┐                ┌──────────────┐
     │ Generate     │                │ Enter        │
     │ Premium Key  │                │ reason       │
     │              │                │              │
     │ PRM-ABC...   │                │ e.g. "Dup    │
     │              │                │ payment"     │
     └──────────────┘                └──────────────┘
           │                                   │
           ▼                                   ▼
┌──────────────────────────────┐  ┌──────────────────────────┐
│ API: validate_purchase       │  │ API: validate_purchase   │
│ {                            │  │ {                        │
│  "status": "approved"        │  │  "status": "rejected"    │
│ }                            │  │  "reason": "reason"      │
└──────────────────────────────┘  └──────────────────────────┘
           │                                   │
           ▼                                   ▼
     ┌──────────────┐                ┌──────────────┐
     │ DB Updates:  │                │ DB Updates:  │
     │ 1. Generate  │                │ 1. Mark      │
     │    key       │                │    rejected  │
     │ 2. Insert    │                │ 2. Log       │
     │    premium_  │                │    reason    │
     │    keys      │                │ 3. Timestamp │
     │ 3. Mark      │                └──────────────┘
     │    approved  │
     │ 4. Timestamp │
     └──────────────┘
           │
           ▼
     ┌──────────────────┐
     │ Return Key:      │
     │ PRM-ABC123...    │
     └──────────────────┘
           │
           ▼
     ┌──────────────────────────────┐
     │ Admin copies key and sends   │
     │ to customer via email/chat   │
     └──────────────────────────────┘
           │
           ▼
     ┌──────────────────────────────┐
     │ Customer receives key        │
     │ and redeems in app           │
     └──────────────────────────────┘
```

### 3. Chat Code Delivery Flow

```
┌──────────────────────────────────┐
│ User generates code              │
│ Click "Deploy Spec"              │
└──────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ Code appears in "Terminal Port"  │
└──────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ User clicks "Chat Support"       │
└──────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ Chat Support modal opens         │
│ Shows message history            │
└──────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ User types message (optional)    │
│ e.g., "I need this for project"  │
└──────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ User clicks "Send"               │
└──────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ API: chat_send                   │
│ {                                │
│   "username": "user",            │
│   "message": "text",             │
│   "code": "full code",           │
│   "timestamp": "2026-01-16..."   │
│ }                                │
└──────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ Database: Insert chat_history    │
│ - username: user                 │
│ - message: text                  │
│ - code: full code stored         │
│ - timestamp: now                 │
│ - status: delivered              │
└──────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ Chat shows:                      │
│ "✓ Code delivered successfully." │
└──────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ Message appears in chat history  │
│ with timestamp                   │
└──────────────────────────────────┘
```

---

## Component Architecture

```
App.tsx (Main Component)
├── Header
│   ├── Logo
│   ├── Navigation (Designs, Archive, Chat Support)
│   └── User Button (Login/Profile)
│
├── Main Content
│   ├── Left Sidebar (Config)
│   │   ├── Category Select
│   │   ├── Execution Select
│   │   ├── Database Select
│   │   └── Deploy Spec Button
│   │
│   └── Right Panel (Code Output)
│       ├── Features Textarea
│       └── Terminal Port (Code Display)
│
├── Modals
│   ├── Auth Modal (Login/Signup)
│   ├── Templates Modal (Project Templates)
│   ├── History Modal (Code History)
│   ├── Profile Modal (User Info)
│   └── Chat Support Modal [NEW]
│       ├── Message History
│       ├── Send Message Input
│       ├── Redeem Code Section
│       └── Confirmation Messages
│
└── Footer
    ├── Copyright
    └── Footer Links

AdminApp.tsx (Admin Component) [NEW]
├── Header
│   ├── Title "Purchase Validator"
│   └── Logout Button
│
├── Main Content
│   ├── Left Panel (Purchase List)
│   │   ├── Filter Buttons (All/Pending/Approved/Rejected)
│   │   ├── Refresh Button
│   │   └── Purchase Queue (Scrollable)
│   │
│   └── Right Panel (Details & Actions)
│       ├── Purchase Details
│       ├── QR Code Generator
│       ├── File Upload (Receipt)
│       ├── Approve Button (Green)
│       └── Reject Input + Button (Red)
│
└── Login Screen [if not authenticated]
    ├── Logo
    ├── Admin Key Input
    └── Unlock Dashboard Button
```

---

## Database Relationships

```
users (1) ──┬──< (∞) prompt_history
            │
            ├──< (∞) chat_history
            │
            └──< (∞) premium_keys (redeemed_by)

premium_keys (1) ──< (∞) purchases (key)
```

---

## Deployment Architecture

```
GitHub Repository
├── App.tsx
├── AdminApp.tsx
├── netlify/functions/api.ts
└── ... (other files)
           │
           │ Push to main
           ▼
Netlify CI/CD
├── Build: npm run build
│   ├── Generates dist/index.html (main app)
│   ├── Generates dist/admin.html (admin app)
│   └── Bundles assets
│
├── Functions
│   └── Deploys api.ts to /.netlify/functions/api
│
└── Deploy
    ├── dist/ → CDN
    ├── Functions → Serverless
    └── Env Vars → Secrets
           │
           ▼
    Production
    ├── https://yoursite.netlify.app/ (Main App)
    ├── https://yoursite.netlify.app/admin/ (Admin)
    └── /.netlify/functions/api (Backend)
           │
           ▼
    Neon PostgreSQL Database
```

---

## Security Architecture

```
┌──────────────────────────────────────────────┐
│            SECURITY LAYERS                   │
└──────────────────────────────────────────────┘

1. Transport Layer
   ├─ HTTPS/SSL (Netlify default)
   └─ Encrypted in transit

2. Authentication Layer
   ├─ User: username + password
   └─ Admin: ADMIN_KEY (environment variable)

3. API Layer
   ├─ Environment variables not exposed
   ├─ DATABASE_URL hidden
   ├─ API_KEY hidden
   └─ ADMIN_KEY hidden

4. Database Layer
   ├─ Parameterized queries (prevent SQL injection)
   ├─ Foreign key constraints
   ├─ Status validation
   └─ Unique constraints on sensitive fields

5. Business Logic
   ├─ Premium codes one-time use only
   ├─ Status validation before actions
   ├─ Admin key validation
   └─ Timestamp audit trail

6. Deployment
   ├─ Netlify secrets management
   ├─ No hardcoded credentials
   ├─ Environment isolation
   └─ Automatic backups
```

---

## Performance Optimization

```
Database Level
├─ Indexes on frequently queried columns
│   ├─ premium_keys(status)
│   ├─ purchases(status)
│   ├─ chat_history(username, timestamp)
│   └─ prompt_history(username, timestamp)
│
└─ Optimal query plans

Application Level
├─ React component optimization
├─ Lazy modal loading
├─ Debounced inputs
└─ Efficient re-renders

Deployment Level
├─ CDN caching for static assets
├─ Netlify edge functions
├─ Serverless auto-scaling
└─ Database connection pooling
```

---

**Last Updated:** January 16, 2026
**Version:** 1.0.0
